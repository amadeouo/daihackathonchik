import classes from './Auth.module.css'
import {useNavigate} from "react-router-dom";
import {PageWrapper} from "@/shared/page-wrapper/ui";

export const Auth = () => {
  const navigate = useNavigate()

  return (

  )
}